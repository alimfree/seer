<section class="pt-32 sm:pt-44 pb-24 sm:pb-32 px-6 sm:px-8 fade-in-section visible">
  <div class="max-w-4xl mx-auto text-center">
    <h1 class="font-headline text-4xl sm:text-5xl md:text-7xl font-light leading-tight tracking-tight text-on-background mb-6 hero-headline">
      Get paid faster for every NEMT trip.<br /><span class="text-tertiary">We handle the billing.</span>
    </h1>
    <p class="font-body text-lg sm:text-xl text-on-surface-variant leading-relaxed max-w-3xl mx-auto mb-8 hero-subtitle">
      Full-service billing and revenue cycle management built specifically for NEMT operators. Higher clean claim rates. Lower denials. Better cash flow — without adding headcount.
    </p>
    <div class="flex flex-col items-center gap-4 mb-16 sm:mb-20 hero-cta">
      <a
        class="btn-primary px-8 py-4 text-on-primary font-medium rounded-sm shadow-ambient"
        href="/#contact"
      >
        Book a Free Billing Assessment
      </a>
      <a
        class="text-on-surface-variant hover:text-tertiary transition-colors duration-300 underline underline-offset-4 decoration-outline-variant/50"
        href="#how-it-works"
      >
        Or scroll to see how it works
      </a>
    </div>
    <div class="relative max-w-5xl mx-auto hero-image">
      <div class="rounded-sm overflow-hidden shadow-ambient bg-surface-container-lowest p-6 sm:p-10">
        <svg class="w-full h-auto" viewBox="0 0 800 400" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="Chart showing NEMT operator revenue climbing from January to June, reaching $485K">
          <defs>
            <linearGradient id="heroAreaGradient" x1="0%" y1="0%" x2="0%" y2="100%">
              <stop offset="0%" stop-color="#002434" stop-opacity="0.1" />
              <stop offset="100%" stop-color="#002434" stop-opacity="0" />
            </linearGradient>
          </defs>
          <path d="M 50 350 L 50 300 Q 150 280 200 220 T 350 180 T 500 120 T 650 100 T 750 50 L 750 350 Z" fill="url(#heroAreaGradient)" />
          <line stroke="#DDE2EE" stroke-width="1" x1="50" x2="750" y1="350" y2="350" />
          <line stroke="#DDE2EE" stroke-dasharray="4,4" stroke-width="1" x1="50" x2="750" y1="250" y2="250" />
          <line stroke="#DDE2EE" stroke-dasharray="4,4" stroke-width="1" x1="50" x2="750" y1="150" y2="150" />
          <line stroke="#DDE2EE" stroke-dasharray="4,4" stroke-width="1" x1="50" x2="750" y1="50" y2="50" />
          <path d="M 50 300 Q 150 280 200 220 T 350 180 T 500 120 T 650 100 T 750 50" fill="none" stroke="#002434" stroke-linecap="round" stroke-width="4" />
          <circle cx="750" cy="50" fill="#002434" r="6" />
          <g fill="#002434" font-family="Newsreader, serif" font-size="24" font-weight="500">
            <text x="760" y="45">$485K &#8593;</text>
          </g>
          <g fill="#4B3111" font-family="Inter, sans-serif" font-size="12" opacity="0.6">
            <text x="50" y="375">JAN</text>
            <text x="200" y="375">FEB</text>
            <text x="350" y="375">MAR</text>
            <text x="500" y="375">APR</text>
            <text x="650" y="375">MAY</text>
            <text text-anchor="end" x="750" y="375">JUN</text>
          </g>
        </svg>
      </div>
    </div>
  </div>
</section>

<style>
  /* Staggered fade-up for hero elements — h1 is always visible for LCP */
  .hero-headline {
    animation: hero-settle 0.8s ease-out both;
  }

  .hero-subtitle {
    opacity: 0;
    transform: translateY(10px);
    animation: hero-fade-up 0.6s ease-out 0.3s forwards;
  }

  .hero-cta {
    opacity: 0;
    transform: translateY(10px);
    animation: hero-fade-up 0.6s ease-out 0.5s forwards;
  }

  .hero-image {
    opacity: 0;
    transform: translateY(12px);
    animation: hero-fade-up 0.7s ease-out 0.7s forwards;
  }

  /* Subtle settle — starts fully visible, just eases into final position */
  @keyframes hero-settle {
    from { transform: translateY(6px); opacity: 0.85; }
    to { transform: translateY(0); opacity: 1; }
  }

  @keyframes hero-fade-up {
    to { opacity: 1; transform: translateY(0); }
  }

  @media (prefers-reduced-motion: reduce) {
    .hero-headline,
    .hero-subtitle,
    .hero-cta,
    .hero-image {
      opacity: 1;
      transform: none;
      animation: none;
    }
  }
</style>
